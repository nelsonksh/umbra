# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Compact compiler output (regenerate with `npm run contract:compile`)
pkgs/contract/src/managed/
# Copy of the same output for the browser app (regenerate with `npm run sync-keys`)
pkgs/app/public/managed/

# Logs
*.log
npm-debug.log*
logs/

# Wallet / private-state secrets -- never commit these. Contains serialized
# wallet state (pkgs/cli/wallet-cache/) and private-state LevelDB data
# (secret keys, work hashes, salts) written by CLI runs against a real network.
wallet-cache/
midnight-level-db/

# Env
.env
.env.local

# OS / editor
.DS_Store
.vscode/
.idea/
